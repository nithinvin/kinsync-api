# Copy to .env and fill in real values for local development.
# Never commit the real .env file (it is already gitignored).

# SQLAlchemy async connection URL for PostgreSQL.
DATABASE_URL=postgresql+asyncpg://kinsync:changeme@localhost:5432/kinsync

# Root logger level: DEBUG, INFO, WARNING, ERROR, CRITICAL.
LOG_LEVEL=INFO
